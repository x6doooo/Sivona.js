var PI = Math.PI,
  abs = Math.abs,
  random = Math.random,
  mmax = Math.max,
  mmin = Math.min,
  fillStyle = '#fff',
  strokeStyle = '#000',
  shadowColor = '#333',
  shadowBlur = 20;

function deg2rad(d){
  return d * PI / 180;
}

function rad2deg(r){
  return r * 180 / PI;
}

